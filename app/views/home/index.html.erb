<!-- Main Content -->
<div class="main-content">

  <%= render partial: '/partials/cuisine_and_recipe_type'  %>

  <h1>Receitas mais favoritadas</h1>
  <% @favorites.each do |recipe| %>
      <%= render recipe %>
  <% end %>
  <div class="dishes padd">
      <div class="container">
        <!-- Default Heading -->
        <div class="default-heading">
          <!-- Crown image -->
          <img class="img-responsive" src="/img/crown.png" alt="" />
          <!-- Heading -->
          <h2>Últimas Receitas</h2>
          <!-- Paragraph -->
          <p>Essas receitas acabaram de entrar no CookBook</p>
          <!-- Border -->
          <div class="border"></div>
        </div>
        <div class="row">
          <% @recipes.each do |recipe| %>
          <%= render recipe %>
          <% end %>
        </div>
        <div class="text-center">
                <%= link_to 'Todas as Receitas', recipes_path, class: "dish-all btn btn-primary btn-lg" %>
        </div>
        <div class="text-center">
                <%= link_to 'Enviar uma receita', new_recipe_path, class: "dish-all btn btn-primary btn-lg" %>
        </div>
      </div>
    </div>
</div>
